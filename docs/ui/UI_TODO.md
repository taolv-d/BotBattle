# UI 临时 TODO

**更新日期**: 2026-03-25  
**用途**: 记录当前 UI 方案中仍不完善、未说清楚或需要重新决策的点。后续讨论以本文件为中心持续修改，直到 UI 设计足够完整、具体、合理。

---

## 1. 后端与前端的边界

- [x] 明确 UI 只是可选观察/交互层，后端必须可以完全脱离前端独立运行
- [x] 明确核心层、适配层、展示层三层边界
- [x] 明确游戏逻辑、状态推进、规则判断、Agent 决策属于后端
- [x] 明确前端只负责展示、收集输入、调用后端接口
- [x] 明确权限隔离必须在后端完成，不能只靠前端隐藏
- [x] 明确日志系统不是 UI 正式接口，只能作为调试辅助
- [x] 明确未来 Web 支持应通过新增适配层接入，而不是侵入核心逻辑
- [x] 明确后续是否允许 UI 代码进入现有入口文件，还是必须新增独立适配层

## 2. 游戏驱动方式

- [x] 明确“谁驱动游戏推进”
- [x] 明确当前 CLI 驱动流程如何演进为“可被 UI 接管但不依赖 UI”的流程
- [x] 明确游戏主循环中哪些节点会阻塞等待输入，哪些节点可以自动推进
- [x] 明确后端需要统一的运行控制器，而不是让 CLI 或 UI 直接驱动编排器细节
- [x] 细化运行状态机与状态迁移图

## 3. 输入适配层

- [x] 设计统一的输入抽象，例如 `InputProvider`
- [x] 明确 CLI 输入、未来 UI 输入、测试输入共用同一接口
- [x] 明确只有在需要用户输入时系统才暂停等待输入
- [x] 明确上帝模式不需要用户输入，游戏应自动持续推进
- [x] 明确玩家发言、投票、技能选择应使用统一输入请求协议
- [x] 定义输入适配层的最小接口职责
- [x] 明确真人超时、取消、默认行为如何处理
- [x] 明确输入请求与游戏事件之间的关联 ID 规则

## 4. 状态模型

- [x] 定义 UI 需要读取的“状态快照”结构
- [x] 区分上帝视角状态与玩家视角状态
- [x] 明确哪些字段是公开状态，哪些字段是受限状态
- [x] 明确状态快照的更新频率与获取方式
- [x] 明确状态快照优先采用完整覆盖式
- [x] 明确后续是否需要支持局部状态更新

## 5. 事件模型

- [x] 定义 UI 需要消费的“事件流”结构
- [x] 区分状态快照与事件流，不让日志直接充当 UI API
- [x] 明确最小事件集合：发言、投票、夜晚行动、死亡、阶段切换、游戏结束、复盘状态
- [x] 明确事件需要稳定 ID、顺序号、时间戳
- [x] 明确事件保留策略与前端补拉方式
- [x] 明确断线恢复优先依赖完整状态快照
- [x] 明确事件缓存上限与清理策略

## 6. 视角隔离

- [x] 定义上帝视角可见信息范围
- [x] 定义玩家视角可见信息范围
- [x] 明确 Agent 建议是否只对真人本人可见
- [x] 明确如何从后端层面保证不把上帝视角数据泄露给玩家视角

## 7. 玩家模式设计

- [x] 明确玩家模式在当前项目中的目标边界
- [x] 明确真人参与时的完整交互流程
- [x] 明确“Agent 建议”的生成时机、展示方式、是否允许忽略
- [x] 明确玩家模式是否要求实时推送，还是允许轮询

## 8. 复盘与 UI 的关系

- [x] 明确 UI 是否需要展示复盘生成状态
- [x] 明确 UI 展示复盘时需要哪些字段：状态、摘要、路径、错误信息
- [x] 明确复盘报告是在 UI 内渲染，还是只提供文件入口

## 9. 通信方案

- [x] 决定第一阶段采用“状态轮询 + 事件补拉”为主方案
- [x] 明确为什么第一阶段不优先选择 SSE / WebSocket
- [x] 明确第一阶段与第二阶段不必强制使用同一通信方案
- [x] 评估第二阶段玩家模式是否需要在此基础上增加推送能力

## 10. 启动与会话模型

- [x] 明确游戏创建、启动、运行、结束的后端生命周期
- [x] 明确存在“创建游戏实例”这一步
- [x] 明确复盘配置、模式选择等应作为对局级配置
- [x] 明确第一阶段采用单进程单活动对局
- [x] 明确 `session_id` 与 `game_id` 的关系和命名规则

## 11. 调试能力

- [x] 明确上帝模式当前默认不引入暂停控制逻辑
- [x] 明确后端需要为上帝模式暂停 / 继续预留扩展接口
- [x] 明确未来 UI 是否需要“暂停 / 继续 / 单步推进”
- [x] 明确 UI 是否需要展示内心独白、详细日志、决策上下文
- [x] 明确调试能力和玩家模式是否必须彻底分离

## 12. 技术实现路线

- [x] 明确第一阶段先做后端契约，还是先做页面原型
- [x] 明确是否新增 `web/` 或其他目录承载 UI 适配代码
- [x] 明确要不要先做一个只读调试页作为 MVP

## 13. 第一阶段后端 API 边界

- [x] 明确第一阶段最小 API 集合
- [x] 区分必须接口与后续扩展接口
- [x] 明确每个接口的职责、输入、输出与失败行为
- [x] 明确哪些接口只用于上帝视角，哪些未来可复用到玩家模式

## 14. 文档补强

- [x] 把 UI 方案中的“目标、非目标、约束、接口契约、状态机、事件模型”补齐
- [x] 删除仍然像“伪实现文档”的表述
- [x] 在 UI 文档中补上与当前狼人杀实现文档的交叉引用

## 15. 正式契约草案

- [x] 将状态快照整理为正式字段契约
- [x] 将事件结构整理为正式字段契约
- [x] 将输入请求 / 输入响应整理为正式字段契约
- [x] 将第一阶段 API 整理为接口级契约草案

---

## 当前建议优先级

建议我们下一轮优先讨论这 4 项：

1. 是否需要在文档中补更细的错误码枚举示例
2. 是否需要把 MVP 后台页拆成更明确的组件清单
3. 是否要开始把文档结论落到代码实现
4. 是否需要把当前 UI 文档再裁成一份更短的实施说明

这四项已经不再是架构级阻塞，而是“继续打磨文档”还是“开始实现”的选择题。按当前完整度，这份 UI 文档已经可以进入实现准备阶段。
